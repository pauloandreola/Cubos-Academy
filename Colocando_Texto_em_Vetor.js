// Colocando texto em um vetor

const frase = "Eu gosto de verão";

let palavra = "";

for (let i = 0; i < frase.length; i++) {
  palavra += frase[i]
  console.log(palavra);
  //Colocando a string contida em frase //dentro de palavra caracter por caracter 
}